package com.example.chat_example;import ch.qos.logback.core.model.Model;import org.springframework.stereotype.Controller;import org.springframework.web.bind.annotation.*;@Controller@RequestMapping("/chat") // 클래스 아래 모든 메서드에 @RequestMapping 이 적용된다public class ChatController {    ChatMessage message = new ChatMessage("김철수","123wqr");    // 2. RsData에서 제너릭한 타입을 ChatMessage로 정한후 불러오면서 사용함    @GetMapping("/writeMessage")    @ResponseBody    public RsData<ChatMessage> writeMessage() {        return new RsData<>("S-1", "메세지가 작성됨", message);    } // 하는이유 = 보고서를 보고 한눈에 오류내용을 확인 할 수 있게 하려고}